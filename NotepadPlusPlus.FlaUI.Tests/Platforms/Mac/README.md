This folder is reserved for a future macOS TextEdit adapter.

The current suite runs through the Windows implementation in `Platforms/Windows`, while the shared interfaces under `Abstractions/` define the contract a macOS lane would implement later.